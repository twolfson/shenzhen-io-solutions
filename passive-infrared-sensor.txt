# Input variables
# rtc, 0 to 100
# sensor, 0 to 100
# on time, -999 to 999
# off time, -999 to 999

# Output variables
# alarm, 0 to 100

# Gist:
# Controller 1
# Check if sensor is at least 20
tgt SENSOR 20
  # If not, reset our alarm
  # DEV: We move `-` here to avoid other comparisons
- mov 0 _ALARM
  # Otherwise
  # If we are
+ mov 100 _ALARM
      # Send alarm signal to controller 2
